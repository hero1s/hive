--router_cfg.lua
--luacheck: ignore 631

--导出配置内容
return {
    {
        host_id = 1, --[[ ip地址 ]]
        host = '127.0.0.1', --[[ ip地址 ]]
        count = 1, --[[ host路由部署数量 ]]
        port = 9001, --[[ 端口 ]]
    },
}
